<%- include("./header.ejs") %>
<%- include("./navbar.ejs") %>
    <li class="nav-item"><a href="/accomodation" class="nav-link">Accomodations</a></li>
    <li class="nav-item"><a href="<% if(userType === 'user'){ %>/register<% }else{ %>/host-register<% } %>" class="nav-link">Register/Log In</a></li>
  </ul>
</nav>
<main class="main">
  <section class="sec" id="sec1">
    <h1 class="page-title"><%= pageTitle %></h1>
    <div class="registration">
      <h4>Enter the follwing details -</h4>
      <form action="<% if(userType === 'user'){ %>/login<% }else{ %>/host-login<% } %>" method="post">
        <div class="form-group">
          <label for="username" class="form-label">Username</label> <br>
          <input type="text" name="username" id="username" class="form-control" placeholder="enter your username">
        </div>
        <div class="form-group">
          <label for="password" class="form-label">Password</label> <br>
          <input type="password" name="password" id="password" class="form-control" placeholder="enter your password">
        </div>
        <div class="form-group">
          <button type="submit" class="btn btn-success">Log In</button>
          <button type="reset" class="btn btn-danger">Cancel</button>
        </div>
      </form>
    </div>
    <div>
      <h4>OR</h4>
      <p>
        not yet registered? register <a href="<% if(userType === 'user'){ %>/register<% }else{ %>/host-register<% } %>">here</a>
      </p>
    </div>
  </section>
</main>

<%- include("./footer.ejs") %>